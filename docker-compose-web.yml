version: "3.0"

services:
  chaturbate-dvr-web:
    container_name: chaturbate-dvr-web
    build:
      context: .
      dockerfile: Dockerfile.web
    environment:
      - GUI_USERNAME=johndoe
      - GUI_PASSWORD=password
    ports:
      - "8080:8080"